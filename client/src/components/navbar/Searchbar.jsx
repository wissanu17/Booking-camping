import { Input } from "../ui/input"

// rafce
const Searchbar = () => {
  return (
    <Input 
        type='text'
        placeholder="Search ... (can't use right now.)"
        className="max-w-xs"
    />
  )
}
export default Searchbar